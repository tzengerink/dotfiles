# Global
alias -g L='| less -S'
alias -g T='| tail'
alias -g J='| python2 -mjson.tool'

# Git
alias g='git'

# Navigation
alias l='ls -l'
alias ll='ls -lAh'
alias lt='ll -rt'              # Sort by date
alias lx='ll -Bx'              # Sort by extension
alias lz='ll -rS'              # Sort by size

# Functions
alias cat='colored_cat'
