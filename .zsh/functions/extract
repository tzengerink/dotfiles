# EXTRACT
# Extract an archive

if [ -f "$1" ]; then
	case "$1" in
		*.tar.gz2)  tar xvjf "$1" ;;
		*.tar.gz)   tar xvzf "$1" ;;
		*.tar)      tar xvf "$1"  ;;
		*.zip)      unzip "$1"    ;;
		*)          echo -e "Unknwon file type. ($1)" ;;
	esac
else
	echo -e "$1 is not a valid file"
fi
