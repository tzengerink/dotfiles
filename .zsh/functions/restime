# RESTIME
# Response time of a host

echo -e "Connect\tStart\tTotal"
echo -e "-------\t-----\t-----"

for (( ; ; ))
do
	OUT=`curl -o /dev/null -s -w "%{time_connect}\t%{time_starttransfer}\t%{time_total}" "$1"`
	echo -e $OUT
done
