# COUNTFILES
# ----------
# Count the number of files (for a given extension).

if [ ! "$1" ]; then
	COUNT=`find . -type f \! -path '*svn*' | wc -l`
else
	COUNT=`find . -type f \! -path '*svn*' -name "*.$1" | wc -l`
fi
echo "$COUNT"
