# SHA1CHECK
# ---------
# Check the SHA-1 checksum provided.

if [ $# -ne 2 ]; then
	echo -e "Usage: $0 [file] [hash]"
else
	hash=$(openssl sha1 $1)
	if [ "SHA1(${1})= $2" = "${hash}" ]; then
		echo "\e[00;32mCORRECT\e[00m"
	else
		echo "\e[00;31mINCORRECT\e[00m"
	fi
fi
