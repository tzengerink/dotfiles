# PASSGEN
# Generate random password string of X length

if [[ $1 != '' ]]; then
	local LANG=C
	local LEN=$1
	tr -dc "A-Za-z0-9" </dev/urandom | head -c ${LEN} | xargs
else
	echo -e "Usage: $0 [password length]"
fi
