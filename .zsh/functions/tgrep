# TGREP
# Recursively search for a string (case insensitive)

if test "$1" = ""
then
	echo "Usage: $0 'Search Term'"
else
	find . -type f \! -path "*.svn*" -exec grep -EHni --color=auto "$1" {} \;
fi
