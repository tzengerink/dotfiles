# TGREP
# Recursively search for a string (case insensitive)

if test "$1" = ""
then
	echo "First argument must be a string to search for.."
else
	find . -type f \! -path "*.svn*" -exec egrep -Hni --color "$1" {} \;
fi
