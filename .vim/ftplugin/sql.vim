" ------------------------------------------------------------------------------
" FOLDING SETTINGS
" ------------------------------------------------------------------------------

	set foldlevel=0        " Use X levels of folding
	set foldmarker=(,)     " Set brackets as default fold marker
	set foldmethod=marker  " Method of folding
	set foldminlines=2     " Do not fold when less then X lines
